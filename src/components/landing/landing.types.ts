export interface FeatureCardProps {
  title: string;
  description: string;
}

export interface CourseCardProps {
  id: string;
  title: string;
  instructor: string;
  price: number;
  tag: string;
}
